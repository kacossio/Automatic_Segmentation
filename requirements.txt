# Grounding DINO (via HF Transformers) + SAM2 stack
transformers>=4.49
sam-2 @ git+https://github.com/facebookresearch/sam2.git
supervision>=0.22.0
pycocotools>=2.0

# Core
PyYAML>=6.0
tqdm>=4.0
Pillow>=9.0

# Install PyTorch with CUDA *before* running pip install -r requirements.txt.
# Example (CUDA 12.4):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# On first run, both Grounding DINO and SAM2 weights auto-download to
# ~/.cache/huggingface/. Models are configurable in config.yaml via
# dino_model (default IDEA-Research/grounding-dino-tiny) and
# sam2_model (default facebook/sam2-hiera-large).
