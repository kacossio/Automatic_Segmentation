# SAM 3 (via HF Transformers) stack
transformers>=4.49  # must include SAM 3 (Sam3Model / Sam3Processor)
supervision>=0.22.0
pycocotools>=2.0

# Core
PyYAML>=6.0
tqdm>=4.0
Pillow>=9.0

# Annotation review UI (review.py)
streamlit>=1.30
streamlit-image-coordinates>=0.1.6
streamlit-shortcuts>=0.1.9  # optional keyboard nav; review.py degrades if absent

# Automated annotation verification (verify.py)
anthropic>=0.40

# Install PyTorch with CUDA *before* running pip install -r requirements.txt.
# Example (CUDA 12.4):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# On first run, SAM 3 weights auto-download to ~/.cache/huggingface/. The
# model is configurable in config.yaml via sam3_model (default facebook/sam3).
